# دليل تسجيل الدخول - نظام إدارة المبيعات

## بيانات الدخول الافتراضية

**المدير:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin`

## ما يحدث عند فتح التطبيق

1. يتم تشغيل دالة `ensureDefaultAdmin()` تلقائياً
2. يتم التحقق من وجود مستخدم admin في Supabase
3. إذا لم يكن موجوداً، يتم إنشاؤه تلقائياً
4. يظهر في الواجهة رسالة "النظام جاهز! استخدم admin/admin لتسجيل الدخول"

## خطوات تسجيل الدخول

1. افتح التطبيق في المتصفح
2. انتظر حتى تظهر رسالة "النظام جاهز"
3. اكتب في حقل اسم المستخدم: **admin**
4. اكتب في حقل كلمة المرور: **admin**
5. اضغط زر "تسجيل الدخول"

## إذا لم يعمل تسجيل الدخول

### الخطوة 1: افتح Console المتصفح
- اضغط F12 على لوحة المفاتيح
- اختر تبويب "Console"

### الخطوة 2: ابحث عن الرسائل
ستجد رسائل تبدأ بـ `[v0]` تشرح ما يحدث:

```
[v0] 🚀 بدء تهيئة النظام...
[v0] 🔍 التحقق من وجود مستخدم admin...
[v0] ✅ المستخدم admin موجود بالفعل
أو
[v0] 📝 إنشاء المستخدم الافتراضي admin...
[v0] ✅ تم إنشاء المستخدم الافتراضي
```

### الخطوة 3: عند محاولة تسجيل الدخول
ستظهر رسائل مثل:

```
[v0] 🔐 محاولة تسجيل الدخول للمستخدم: admin
[v0] 🔍 محاولة تسجيل الدخول: {username: 'admin', password: 'admin'}
[v0] 🧹 بعد التنظيف: {cleanUsername: 'admin', cleanPassword: 'admin'}
[v0] ✅ تم تسجيل الدخول بنجاح: admin
```

### الخطوة 4: إذا ظهرت رسائل خطأ

**إذا ظهر: "❌ المستخدم غير موجود"**
- يعني أن المستخدم admin غير موجود في Supabase
- سيتم إنشاؤه تلقائياً
- حاول تسجيل الدخول مرة أخرى

**إذا ظهر: "📋 جميع المستخدمين في قاعدة البيانات: []"**
- يعني أن قاعدة البيانات فارغة
- تحقق من اتصال Supabase
- انسخ الرسائل وأرسلها لي

**إذا ظهر: "❌ خطأ في تسجيل الدخول"**
- قد يكون هناك مشكلة في الاتصال بـ Supabase
- تحقق من ملف `lib/supabase-client.ts`
- تأكد من صحة SUPABASE_URL و SUPABASE_ANON_KEY

## التحقق من Supabase

1. افتح موقع Supabase: https://supabase.com
2. سجل دخول لمشروعك
3. اذهب إلى **Table Editor**
4. افتح جدول **users**
5. ابحث عن مستخدم اسمه "admin"
6. تأكد من:
   - username = "admin"
   - password = "admin"
   - role = "admin"
   - is_active = true

## إنشاء المستخدم يدوياً في Supabase

إذا لم ينجح الإنشاء التلقائي:

1. اذهب إلى Supabase > SQL Editor
2. انسخ والصق هذا الكود:

```sql
-- حذف المستخدم القديم إذا وجد
DELETE FROM users WHERE username = 'admin';

-- إنشاء المستخدم الجديد
INSERT INTO users (username, password, full_name, role, is_active, admin_permission)
VALUES ('admin', 'admin', 'المدير العام', 'admin', true, 'full');
```

3. اضغط زر **Run**
4. ارجع للتطبيق وحاول تسجيل الدخول

## المزامنة بين الأجهزة

بعد نجاح تسجيل الدخول، النظام يستخدم Supabase للمزامنة:
- ما تضيفه من الحاسوب يظهر على الهاتف
- ما يضيفه المندوب من الهاتف يظهر على الحاسوب
- التحديث يحدث تلقائياً كل 5 ثوان

## دعم فني

إذا استمرت المشكلة:
1. افتح Console (F12)
2. انسخ جميع الرسائل التي تبدأ بـ `[v0]`
3. أرسلها لي لتحليل المشكلة
